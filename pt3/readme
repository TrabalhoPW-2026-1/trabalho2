npx prisma migrate dev

mysql -uroot -psenhasegura -h127.0.0.1 -P3306 game

npx prisma migrate dev --name create-major-table

npx prisma generate

npx prisma init --datasource-provider mysql

npm i @prisma/client @prisma/adapter-mariadb

npm i -D prisma

docker run -d \
  --name game-app-phpmyadmin \
  --network game-app-network \
  -e PMA_HOST=game-app-db \
  -e PMA_PORT=3306 \
  -e PMA_USER=root \
  -e PMA_PASSWORD=senhasegura \
  -p 8081:80 \
  phpmyadmin/phpmyadmin

docker run -d \
  --name game-app-db \
  --network game-app-network \
  -p 3307:3306 \
  -e MYSQL_ROOT_PASSWORD=senhasegura \
  -e MYSQL_DATABASE=game \
  -v game-app-volume:/var/lib/mysql \
  mysql:latest

docker network create game-app-network
